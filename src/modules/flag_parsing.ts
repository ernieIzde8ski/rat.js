module.exports = (args: Array<string>): Array<Array<string | object> | undefined> => {
    if (args === [] || args === undefined) return [args, undefined];

    let full_args = "";
    for (let arg of args) {
        arg
    }
}